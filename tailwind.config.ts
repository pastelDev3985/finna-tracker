import type { Config } from "tailwindcss";

/** Supplements CSS-first theme in `app/globals.css`. `darkMode: "class"` matches `next-themes` + `.dark` on `<html>`. */
export default {
  darkMode: "class",
} satisfies Config;
